<!DOCTYPE html>
<html lang="en">
<head>
  <!-- ... meta tags e links de estilo ... -->
</head>
<body>
  <main class="container my-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="mb-0">financeiro</h1>
        <%= link_to 'Novo financeiro', new_financeiro_path, class: "button" %>

        
    </div>

   

    <div class="row g-4">
        <% @financeiros.each do |financeiro| %>
        <div class="col-md-6 col-lg-4">
            <section class="card">
                <img src="https://img.freepik.com/free-photo/3d-illustration-calendar-page-clock-money_107791-15945.jpg?t=st=1719432544~exp=1719436144~hmac=d9d6dea1927d733dc140a5c6188a328801aca0c12d15520764a89d2f23906160&w=1060" class="card-img-top" alt="Ilustração de financeiro">
                <div class="card-body">
                    <h5 class="card-title"><%= financeiro.titulo %></h5>
                    <p class="card-text"><strong>Local:</strong> <%= financeiro.data_paga.strftime("%d/%m/%Y %H:%M") %></p>
                    <p class="card-text"><strong>Data e Hora:</strong> <%= financeiro.data_vencimento.strftime("%d/%m/%Y %H:%M") %></p>
                    <p class="card-text"><strong>Status:</strong> <%= financeiro.status ? "Paga" : "Não paga" %></p>

                    <div class="d-flex justify-content-between align-items-center">
            <%= link_to 'Mostrar', financeiro, class: "btn btn-sm btn-outline-primary" %>
            <%= link_to 'Editar', edit_financeiro_path(financeiro), class: "btn btn-sm btn-outline-secondary" %>
            <% if financeiro.status %>
              <span class="btn btn-sm btn-outline-success">Pago</span>
            <% else %>
              <%= button_to 'Marcar como Pago', pago_financeiro_path(financeiro), method: :patch, data: { confirm: 'Tem certeza?' }, class: "btn btn-sm btn-outline-success" %>
            <% end %>
          </div>
                </div>
            </section>
        </div>
        <% end %>
    </div>

    <%= link_to 'Voltar', root_path %>
  </main>

  <!-- Scripts Bootstrap -->
</body>
</html>
