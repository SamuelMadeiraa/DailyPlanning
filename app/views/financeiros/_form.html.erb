<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DailyPlanning - Novo financeiro</title>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <style>
        body {
            background-color: #f4f4f9;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            font-family: 'Alata', sans-serif;
        }

        .form-container {
            background-color: #ffffff;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 20px;
            max-width: 500px;
            width: 100%;
        }

        .form-container h2 {
            color: #333333;
            margin-bottom: 20px;
        }

        .form-container .field {
            margin-bottom: 15px;
        }

        .form-container label {
            color: #666666;
            margin-bottom: 5px;
        }

        .form-container input[type="text"],
        .form-container input[type="datetime-local"] {
            border: 1px solid #dddddd;
            border-radius: 5px;
            width: 100%;
            padding: 10px;
            transition: border-color 0.3s;
        }

        .form-container input[type="text"]:focus,
        .form-container input[type="datetime-local"]:focus {
            border-color: #B7D3FA;
        }

        .form-container .actions {
            display: flex;
            justify-content: center;
        }

        .form-container .actions input[type="submit"] {
            background-color: #D077AF;
            color: #ffffff;
            border: none;
            border-radius: 5px;
            padding: 10px 20px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        .form-container .actions input[type="submit"]:hover {
            background-color: #B7D3FA;
        }

        #error_explanation {
            background-color: #f8d7da;
            color: #721c24;
            border: 1px solid #f5c6cb;
            border-radius: 5px;
            padding: 10px;
            margin-bottom: 20px;
        }

        #error_explanation h2 {
            margin-top: 0;
        }

        #error_explanation ul {
            list-style: none;
            padding-left: 0;
        }

        #error_explanation ul li {
            margin-left: 0;
        }
    </style>
</head>
<body>
    <div class="form-container">
        <%= form_with(model: @financeiro, local: true, class: "form") do |form| %>
            <h2>Nova finança</h2>
            <% if @financeiro.errors.any? %>
                <div id="error_explanation">
                    <h2><%= pluralize(@financeiro.errors.count, "erro") %> impedem que este financeiro seja salvo:</h2>
                    <ul>
                        <% @financeiro.errors.full_messages.each do |message| %>
                            <li><%= message %></li>
                        <% end %>
                    </ul>
                </div>
            <% end %>

            <div class="field">
                <%= form.label :titulo, "Título" %>
                <%= form.text_field :titulo, class: "form-control" %>
            </div>

              <div class="field">
                <%= form.label :data_paga, "Data paga" %>
               <%= form.datetime_local_field :data_paga, class: "form-control" %>
            </div>

            <div class="field">
                <%= form.label :data_vencimento, "Data vencimento" %>
                 <%= form.datetime_local_field :data_vencimento, class: "form-control" %>
            </div>


            <div class="actions">
                <%= form.submit "Salvar", class: "btn btn-primary" %>
            </div>
        <% end %>
    </div>

    <!-- Bootstrap JS (opcional) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
